# AGENTS.md

## 系统级补充规则

- 当网络直连失败（如超时、DNS 解析失败、连接被拒绝）时，自动启用系统代理 `127.0.0.1:7897` 并重试一次。
- 代理地址统一使用 `http://127.0.0.1:7897`，并同步到 `HTTP_PROXY`、`HTTPS_PROXY`、`ALL_PROXY`。
- 本地地址不走代理：`NO_PROXY=localhost,127.0.0.1,::1`。
